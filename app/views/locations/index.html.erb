<%= content_for(:navbar_class, "navbar-home") %>

<div class="banner">
  <div class="banner-content text-center">
    <div class="home-title">
      <h1>Tu veux dormir chez nous?</h1>
      <h3>Fais un choix!</h3>
    </div>
    <div class="home-buttons">
      <% @locations.each do |location| %>
        <%= link_to "#{location.name}", location_path(location), class: "btn home-btn"%>
      <% end %>
    </div>
  </div>
</div>


