<%= simple_form_for [@booking] do |f| %>
  <%= f.input :start_date %> <!-- use datepicker on here -->
  <%= f.input :end_date %>
  <%= f.input :place %> <!-- make this a slider -->
  <%= f.input :message %>
  <%= f.submit "modifier", class: "btn btn-primary" %>
<% end %>

<%= link_to "Annuler", location_path(@location) %>
